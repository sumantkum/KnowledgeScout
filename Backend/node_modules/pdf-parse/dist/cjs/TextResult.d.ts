import { InfoResult } from './InfoResult.js';
export interface PageTextResult {
    num: number;
    text: string;
}
export declare class TextResult implements InfoResult {
    pages: Array<PageTextResult>;
    text: string;
    total: number;
    getPageText(num: number): string;
    constructor(info: InfoResult);
}
